# MatchGen Backend Environment Configuration
# Copy this file to .env and update with your actual values

# Email Configuration (Gmail - Recommended)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Frontend URL (update this to your actual frontend URL)
FRONTEND_URL=http://localhost:3000

# Database Configuration
# For local development (SQLite)
DATABASE_URL=sqlite:///db.sqlite3

# For production (PostgreSQL on Railway)
# DATABASE_URL=postgresql://username:password@host:port/database

# Secret Key (IMPORTANT: Generate a new secret key for production)
# You can generate one at: https://djecrety.ir/
SECRET_KEY=django-insecure-change-this-to-a-real-secret-key-in-production

# Debug Mode (set to False in production)
DEBUG=True

# Allowed Hosts (add your production domain here)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# CORS Settings (add your frontend domains here)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Security Settings (for development)
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Railway Configuration (if deploying to Railway)
# RAILWAY_STATIC_URL=your-railway-static-url
# RAILWAY_PUBLIC_DOMAIN=your-railway-domain.railway.app